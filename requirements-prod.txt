# Production-only dependencies for the trading agent.
# Excludes dev tools (pytest), notebooks (jupyter, plotly), and
# backtesting-only packages (vectorbt, ta) to keep the image small.

# Broker APIs
alpaca-py

# Data
yfinance
pandas
numpy

# Utilities
python-dotenv
loguru
pyyaml

# Web scraping (Capitol Trades)
requests
beautifulsoup4
fake-useragent
tenacity

# Scheduling
APScheduler>=3.10

# Slack notifications (websocket-client enables Socket Mode for interactive buttons)
slack-sdk
websocket-client

# Database
sqlalchemy
